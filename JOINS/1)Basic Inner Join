/* Simple INNER JOIN:
Write a query to fetch the employee name and their department name from the employees and departments tables.

Tables:
employees(emp_id, emp_name, dept_id)
departments(dept_id, dept_name)
*/

SELECT employees.emp_name, departments.dept_name
FROM employees
JOIN departments
ON employees.dept_id = departments.dept_id;
